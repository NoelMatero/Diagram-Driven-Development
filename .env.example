# Port for the live board. Anything that is not a port between 1 and 65535 is
# refused rather than coerced, so a typo says so instead of quietly serving
# somewhere else.
BOARD_PORT=4747

# Root the MCP server confines every path to. Defaults to the working directory;
# no tool will read or write outside it.
# BOARD_MCP_ROOT=
